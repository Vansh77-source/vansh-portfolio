import './Projects.css';
const GITHUB = 'https://github.com/Vansh77-source';
const ADVOCATE_BRIEF = 'https://www.worldclasslegal.com/';
const PROJECTS = [{ index: '01', name: 'Advocate Brief', type: 'Case management tool', desc: 'A focused workspace for advocates to keep clients, active cases and upcoming court dates in one place.', role: 'UI Design · Frontend Development', tags: ['React', 'Bootstrap', 'JavaScript'], tone: 'brief' }, { index: '02', name: 'Campus Connect', type: 'Student portal', desc: 'A centralized portal that brings campus information, resources and updates into one connected student interface.', role: 'Frontend Development', tags: ['React', 'HTML', 'CSS'], tone: 'campus' }];
function Preview({ project }) { return <div className={`project-preview project-preview--${project.tone}`} aria-hidden="true"><span className="project-preview__chrome"><i /><i /><i /></span><div className="project-preview__nav"><b>{project.name === 'Advocate Brief' ? 'brief.' : 'campusconnect'}</b><span>Dashboard</span><span>Updates</span></div><div className="project-preview__content"><div><small>{project.type}</small><strong>{project.name === 'Advocate Brief' ? 'Every case, in view.' : 'Campus, connected.'}</strong><em /></div><div className="project-preview__cards"><i /><i /><i /></div></div></div> }
function Projects() { return <section id="projects" className="section projects"><div className="container"><div className="section-head reveal"><p className="eyebrow">04 · selected work</p><h2 className="section-title">Built with <span>intent</span>.</h2><p className="section-desc">A few things I&apos;ve designed, built and shipped.</p></div><div className="projects__list">{PROJECTS.map((project, i) => <article className="project-card reveal" style={{ '--delay': `${i * 110}ms` }} key={project.name}><Preview project={project} /><div className="project-card__content"><span className="project-card__index">{project.index} / selected work</span><h3>{project.name === 'Advocate Brief' ? <a href={ADVOCATE_BRIEF} target="_blank" rel="noopener noreferrer">{project.name}</a> : project.name}</h3><p className="project-card__type">{project.type}</p><p className="project-card__desc">{project.desc}</p><p className="project-card__role">{project.role}</p><div className="project-card__tags">{project.tags.map((tag) => <span key={tag}>{tag}</span>)}</div><div className="project-card__actions"><a href={project.name === 'Advocate Brief' ? ADVOCATE_BRIEF : GITHUB} target="_blank" rel="noopener noreferrer">view application <b>↗</b></a></div></div></article>)}</div></div></section>; }
export default Projects;
