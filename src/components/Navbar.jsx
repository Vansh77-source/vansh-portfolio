import { useEffect, useState } from 'react';
import './Navbar.css';
const LINKS = [{ label: 'about', href: '#about' }, { label: 'expertise', href: '#skills' }, { label: 'experience', href: '#experience' }, { label: 'work', href: '#projects' }, { label: 'contact', href: '#contact' }];
function Navbar() { const [scrolled, setScrolled] = useState(false); const [open, setOpen] = useState(false); const [active, setActive] = useState(''); useEffect(() => { const onScroll = () => { setScrolled(window.scrollY > 12); const current = LINKS.findLast(({ href }) => document.querySelector(href)?.getBoundingClientRect().top <= 130); setActive(current?.href || ''); }; onScroll(); window.addEventListener('scroll', onScroll, { passive: true }); return () => window.removeEventListener('scroll', onScroll); }, []); return <header className={`navbar ${scrolled ? 'navbar--scrolled' : ''}`}><div className="container navbar__inner"><a href="#top" className="navbar__logo" onClick={() => setOpen(false)}><span className="navbar__prompt">~/</span>vansh<span className="navbar__cursor" aria-hidden="true" /></a><nav className={`navbar__links ${open ? 'navbar__links--open' : ''}`} aria-label="Main navigation">{LINKS.map((link, i) => <a key={link.href} href={link.href} className={active === link.href ? 'is-active' : ''} onClick={() => setOpen(false)}><span className="navbar__index">0{i + 1}</span>{link.label}</a>)}<span className="navbar__socials"><a href="https://github.com/Vansh77-source" target="_blank" rel="noopener noreferrer" aria-label="Vansh Bijwe on GitHub">gh↗</a><a href="https://www.linkedin.com/in/vansh-bijwe-a578a2275" target="_blank" rel="noopener noreferrer" aria-label="Vansh Bijwe on LinkedIn">in↗</a></span></nav><button className={`navbar__toggle ${open ? 'navbar__toggle--open' : ''}`} onClick={() => setOpen((v) => !v)} aria-label="Toggle menu" aria-expanded={open}><span /><span /><span /></button></div></header>; }
export default Navbar;
